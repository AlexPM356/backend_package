# AUTOMATION BACKEND SERVER

# WARNING
- This package should only be execute by using the npx command such as: npx <this package name>.

# DESCRIPTION
- This package will install a package.json with express and nodemon.